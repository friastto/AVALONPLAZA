package org.frias.avalon.core.configservices;

public abstract class ConfigServiceStaffTenant {
   /* // 1. ¿Es del Staff de Avalon? (Los dueños)
    protected boolean isSaaSStaff() {
        return TenantContext.getTenantId() == null && TenantContext.getOutletId() == null;
    }

    // 2. ¿Es un Gerente/Staff de una Empresa Cliente?
    protected boolean isCompanyStaff() {
        return TenantContext.getTenantId() != null && TenantContext.getOutletId() == null;
    }

    // 3. ¿Es alguien que trabaja en una sucursal específica?
    protected boolean isOutletStaff() {
        return TenantContext.getOutletId() != null;
    }
}

    */
}
