# H2 Configuration (Commented out per explicit user instruction)
# spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
# spring.flyway.enabled=false

# PostgreSQL Real / Testcontainers Configuration
spring.test.database.replace=none
spring.datasource.url=${spring.datasource.url:${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:9800/AvalonApi}}
spring.datasource.username=${spring.datasource.username:${SPRING_DATASOURCE_USERNAME:SoporteAvalonFrias}}
spring.datasource.password=${spring.datasource.password:${SPRING_DATASOURCE_PASSWORD:frias}}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration/global

# =================================
# JWT DEFAULTS FOR TEST PROFILE
# =================================
app.jwt.secret=QXZhbG9uVGVzdFNlY3JldEtleUZvckNvbnRpbnVvdXNJbnRlZ3JhdGlvbjIwMjZTZWN1cmVITUFDU0hBNTEyS2V5TWluaW11bTY0Qnl0ZXMhIQ==
app.jwt.expiration-ms=3600000
app.jwt.refresh-expiration-ms=86400000

# =================================
# AWS DEFAULTS FOR TEST PROFILE (DISABLED)
# =================================
aws.accessKey=test-access-key
aws.secretKey=test-secret-key
aws.region=us-east-1

# =================================
# REMOVEBG DEFAULTS FOR TEST PROFILE
# =================================
removebg.apikey=test-removebg-key

# =================================
# PASSWORD RESET CONFIG FOR TEST
# =================================
app.reset-password.url=http://localhost:8900/reset-password

# =================================
# EMAIL (SMTP) DEFAULTS FOR TEST PROFILE (DISABLED)
# =================================
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test
spring.mail.password=test

# =================================
# BREVO EMAIL API DEFAULTS FOR TEST PROFILE
# =================================
brevo.api.url=https://api.brevo.com/v3/smtp/email
brevo.api.key=test-brevo-api-key
brevo.sender.email=test@avalon.com
brevo.sender.name=Avalon Test

# =================================
# TOMCAT ACCESS LOG (DISABLED FOR TEST)
# =================================
server.tomcat.accesslog.enabled=false
