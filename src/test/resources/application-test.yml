# H2 Configuration (Commented out per explicit user instruction)
# spring:
#   datasource:
#     url: jdbc:h2:mem:avalon_test_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
#     driver-class-name: org.h2.Driver
#     username: sa
#     password: password
#   jpa:
#     hibernate:
#       ddl-auto: create-drop
#     properties:
#       hibernate:
#         dialect: org.hibernate.dialect.H2Dialect

spring:
  test:
    database:
      replace: none
  datasource:
    url: jdbc:postgresql://localhost:9800/AvalonApi
    username: SoporteAvalonFrias
    password: frias
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration/global
